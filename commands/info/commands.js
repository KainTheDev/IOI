const { Command } = require("eris");

module.exports = new Command('commands',
/**
 * 
 * @param {Message} message 
 * @param {*} args 
 */
    async function (message, args) {

    },
    {
        description: 'Show commands list'
    }
)